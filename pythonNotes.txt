>pada windows search, ketik IDLE ...console untuk jalanin python 3.8
>pada Linux pake ctrl+alt+t

> dasar kluarin kata kata tinggal ketik print(" xxxx")  trus tampa semicolon bisa lagsung
>untuk keluar dari terminal ketik exit()
> dasar membaca input keyboard pake input() dan raw_input() 
py 3 raw_input() udah ga berlaku lagi


>isi piton angka, huruf, list[, ], tuple (,) dictionary{x:x,}


exception handling >> https://belajarpython.com/tutorial/exception-python






> extensi file nya *.py
> case sensitive
> masukin komentar di editor pake #
> tipe data yg ada >> string, boolean, integer, hexadecimal, complex, list, tuple,dictionary
> aturan variable py :
   -Karakter pertama harus berupa huruf atau garis bawah/underscore _
   -Karakter selanjutnya dapat berupa huruf, garis bawah/underscore _ atau angka
   -Karakter pada nama variabel bersifat sensitif (case-sensitif). Artinya huruf kecil dan huruf besar dibedakan. Sebagai contoh, variabel namaDepan dan namadepan adalah variabel yang berbeda.
>> syntax >> type(variabel) untuk mengecek tipe data nya
>> penulisan variable ga perlu dideklarasikan.
>> operator aritmatika py >> +, -, *, /, %, **, //
>> operator perbandingan py >> ==,!=, <>, >,<,>=,<=

> kondisi py >> if, else dan elif
pemakaian 
if(kondisi):
      blalalalala
else:
      blalalalalala
indentation di py biasanya peka gaes, ;D
if ( kondisi):
      blalalala
elif (kondisi):
      blalalala

>> Loop py >> while loop, for loop, nested loop





di RasPi > buka terminal ketik sbb
>python ( untuk ver 2)
>python3 (untuk ver 3)
kalo belum keinstall install pake 
>sudo apt-get install python3



flask kalo run venv masi belon bisa bisa ditambah source di depannya.


append()	Adds an element at the end of the list
clear()	Removes all the elements from the list
copy()	Returns a copy of the list
count()	Returns the number of elements with the specified value
extend()	Add the elements of a list (or any iterable), to the end of the current list
index()	Returns the index of the first element with the specified value
insert()	Adds an element at the specified position
pop()	Removes the element at the specified position
remove()	Removes the item with the specified value
reverse()	Reverses the order of the list
sort()	Sorts the list
